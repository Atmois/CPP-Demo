#include <iostream>

using str = std::string;

int main()
{
  for (int i = 0; i <= 10; i++)
  { // First part says where to start, second part what to require before doing the for loop, third
    // part is to increment by 1
    std::cout << i << "\n";
  }

  return 0;
}